# ==============================================================================
# NutriVision AI - Render Deployment Blueprint (render.yaml)
# Custom URLs containing GitHub username: kiranbcrkbc
# ==============================================================================

services:
  # 1. AI Microservice (FastAPI + OpenCV + MobileNetV2 ONNX)
  - type: web
    name: kiranbcrkbc-nutrivision-ai-service
    env: docker
    dockerfilePath: ./ai-service/Dockerfile
    dockerContext: ./ai-service
    plan: free
    healthCheckPath: /api/ai/health
    envVars:
      - key: PORT
        value: 8000
      - key: HOST
        value: 0.0.0.0

  # 2. Backend Gateway (Spring Boot 3 + Security + JWT)
  - type: web
    name: kiranbcrkbc-nutrivision-backend
    env: docker
    dockerfilePath: ./backend/Dockerfile
    dockerContext: ./backend
    plan: free
    healthCheckPath: /api/health
    envVars:
      - key: SPRING_PROFILES_ACTIVE
        value: prod
      - key: AI_SERVICE_BASE_URL
        fromService:
          type: web
          name: kiranbcrkbc-nutrivision-ai-service
          property: host
      - key: SPRING_DATASOURCE_URL
        sync: false
      - key: SPRING_DATASOURCE_USERNAME
        sync: false
      - key: SPRING_DATASOURCE_PASSWORD
        sync: false
      - key: APP_CORS_ALLOWED_ORIGINS
        value: "https://kiranbcrkbc-nutrivision-ai.onrender.com,https://kiranbcrkbc-nutrivision.onrender.com,http://localhost:5173"

  # 3. Frontend UI (React 18 + Vite Static Site)
  - type: web
    name: kiranbcrkbc-nutrivision-ai
    env: static
    rootDir: frontend
    buildCommand: npm install && npm run build
    staticPublishPath: ./dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
    envVars:
      - key: VITE_API_BASE_URL
        value: "https://kiranbcrkbc-nutrivision-backend.onrender.com/api"
